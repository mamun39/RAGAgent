# Assertions

Custom assertion helpers, reusable assertion sets, or assertion notes can live here in later steps.

This baseline setup uses Promptfoo built-in assertions only and does not add custom code yet.
